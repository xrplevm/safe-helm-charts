---
apiVersion: v1

kind: Service

metadata:
  name: {{ include "safe-events-service.fullname" . }}-web

spec:
  selector:
    app: {{ include "safe-events-service.fullname" . }}-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
---
